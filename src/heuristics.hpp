#pragma once

using namespace std;

#include <vector>
#include <string>
#include "node.hpp"

int manhattan_puzzle15(node *u);

int manhattan_puzzle24(node *u);

vector<string> split_state_str(node *u);

